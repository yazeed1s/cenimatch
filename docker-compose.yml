services:
  db:
    build: .
    container_name: cenimatch-db
    environment:
      POSTGRES_USER: u
      POSTGRES_PASSWORD: "112233"
      POSTGRES_DB: cenimatch-db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./data:/data
    restart: unless-stopped

volumes:
  pgdata:
